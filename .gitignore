# Entornos virtuales
.venv/
venv/

# Python
__pycache__/
*.pyc

# Pesos entrenados (pesados; se documentan en README como obtenerlos/copiarlos)
backend/weights/**/*.pth
backend/weights/**/*.pt

# Salidas en runtime
backend/reports/lighting_analysis/*
!backend/reports/lighting_analysis/.gitkeep

# Editor / SO
.vscode/
.idea/
Thumbs.db
.DS_Store

# Frontend (SELENE)
frontend/node_modules/
frontend/dist/
frontend/.env

# Backend (SELENE)
backend/.env

# Base de datos SQLite local (datos reales de usuarios/detecciones/etc.,
# igual que antes no se versionaba el volumen de Postgres). Vive en
# backend/database/data/ (bind mount de Docker, ver docker-compose.yml).
backend/database/*.db
backend/database/data/
